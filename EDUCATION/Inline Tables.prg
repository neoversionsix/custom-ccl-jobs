SELECT
*
FROM
	PRSNL   P
	, (
		(SELECT
			X.PERSON_ID

		FROM
			PERSON   X

		WHERE X.NAME_LAST_KEY = "WHITTLE"

		WITH SQLTYPE("F8"))   I
	)

PLAN P
JOIN I WHERE I.PERSON_ID = P.PERSON_ID

WITH MAXREC = 100, NOCOUNTER, SEPARATOR=" ", FORMAT, TIME = 10