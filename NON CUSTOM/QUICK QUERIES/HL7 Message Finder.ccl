SELECT
	CREATED = O_T.CREATE_DT_TM "DD-MMM-YYYY HH:MM:SS"
	, HL7 = O_T.MSG_TEXT
	, ID = O_T.EVENTID

FROM
	OEN_TXLOG   O_T

PLAN O_T
    WHERE O_T.CREATE_DT_TM > CNVTLOOKBEHIND("10,H")
    AND O_T.MSG_TEXT = "*PID*"

WITH FORMAT, SEPERATOR = " ", TIME = 15