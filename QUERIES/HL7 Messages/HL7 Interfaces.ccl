SELECT
	  OP.INTERFACEID
	, OP.PROC_DESC
	, OP.PROC_NAME
	, OP.CONTROLLABLE
	, OP.CURRENT_NODE
	, OP.PRIMARY_NODE
	, OP.PRIMARY_SCP_EID
	, OP.QUERYABLE
	, OP.ROWID
	, OP.SCP_EID
	, OP.SERVICE
	, OP.STD_INTERFACEID
	, OP.UPDT_APPLCTX
	, OP.UPDT_CNT
	, OP.UPDT_DT
	, OP.UPDT_DT_TM
	, OP.UPDT_ID
	, OP.UPDT_TASK
	, OP.UPDT_TM
	, OP.USER_NAME

FROM
	OEN_PROCINFO   OP

WITH TIME = 10