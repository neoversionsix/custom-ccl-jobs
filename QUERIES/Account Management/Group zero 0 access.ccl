SELECT
	D.CCLCOUNT
	, D.DATESTAMP
	, D.GROUP
	, D.GROUP_NAME
	, D.PLATFORM
	, D.RCODE
	, D.STAT
	, D.TIMESTAMP
	, D.USER_NAME

FROM
	DUAF   D

WITH NOCOUNTER, SEPARATOR=" ", FORMAT, time=10