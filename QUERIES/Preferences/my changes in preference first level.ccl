SELECT
	P.ENTRY_ID
	, P.ROWID
	, P.UPDT_APPLCTX
	, P.UPDT_CNT
	, P.UPDT_DT_TM
	, P.UPDT_ID
	, P.UPDT_TASK
	, P.VALUE
	, P.VALUE_UPPER

FROM
	PREFDIR_CONTEXT   P

WHERE P.UPDT_ID = reqinfo->updt_id

WITH NOCOUNTER, SEPARATOR=" ", FORMAT, TIME = 10