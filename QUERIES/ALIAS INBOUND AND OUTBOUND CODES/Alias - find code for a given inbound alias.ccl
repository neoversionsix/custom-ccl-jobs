SELECT
	C.ALIAS
	, C.CODE_SET
	, C.CODE_VALUE
	, C_CONTRIBUTOR_SOURCE_DISP = UAR_GET_CODE_DISPLAY(C.CONTRIBUTOR_SOURCE_CD)

FROM
	CODE_VALUE_ALIAS   C

WHERE C.ALIAS IN ("MAC","ALRGY", "MULDRUG")

WITH NOCOUNTER, SEPARATOR=" ", FORMAT, TIME = 5